[project]
name = "agent-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "deepagents>=0.6.2",
    "dotenv>=0.9.9",
    "langchain>=1.3.1",
    "langchain-openai>=1.2.1",
]
